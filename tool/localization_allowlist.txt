# Narrow regular expressions matched against `path:line:source` audit output.
# Each entry must be a stable wire/debug/format value, never ordinary UI copy.

# Debug-only local telemetry inspector; excluded from production builds.
^lib/mobile/screens/debug_funnel_screen\.dart:
^lib/mobile/screens/settings_screen\.dart:.*title: 'Funnel'
^lib/mobile/screens/settings_screen\.dart:.*subtitle: 'On-device telemetry counters & recent events'

# Locale-formatted value assembled from caller-supplied localized prefix/suffix.
^lib/widgets/animations\.dart:.*formatter\.format

# Stable example format for an offline desktop license key, not prose.
^lib/widgets/paywall_sheet\.dart:.*DESKTOP-XXXX-XXXX-XXXX

# Stable ICU selector keys for localized Guided ERP interface messages. These
# values select ARB copy and are never presented to the user or persisted.
^lib/mobile/screens/erp_exercises_screen\.dart:.*context\.l10n\.erpFlowText\('[a-zA-Z]+'
^lib/mobile/screens/erp_exercises_screen\.dart:.*\.l10n\.erpPlanText\('[a-zA-Z]+'

# Stable ICU selector keys for Structured Programs. These select localized UI
# messages and never appear to users or enter storage.
^lib/mobile/screens/structured_programs_screen\.dart:.*\.l10n\.structuredProgramText\('[a-zA-Z]+'

# Stable ICU selector keys for the localized Y-BOCS mobile flow. The quoted
# values select ARB copy and are never shown directly or stored.
^lib/mobile/screens/ybocs_screen\.dart:.*\.ybocsText\('[a-zA-Z]+'

# Stable ICU selector keys for immediate-support and breathing copy.
^lib/mobile/screens/emergency_toolkit_screen\.dart:.*\.emergencyToolkitText\('[a-zA-Z]+'
^lib/mobile/screens/exposure_hierarchy_screen\.dart:.*\.exposureHierarchyText\('[a-zA-Z]+'
^lib/mobile/screens/exposure_materials_screen\.dart:.*\.exposureMaterialsText\('[a-zA-Z]+'

# Stable ICU selector keys for the localized Response Prevention and Urge
# Surfing tools. These select ARB copy and are never displayed or persisted.
^lib/mobile/screens/response_prevention_screen\.dart:.*\.responsePreventionText\('[a-zA-Z]+'
^lib/mobile/screens/urge_surf_screen\.dart:.*\.urgeSurfText\('[a-zA-Z]+'

# Stable ICU selector keys for Behavioral Experiments. These select localized
# interface messages and never appear to users or enter storage.
^lib/mobile/screens/behavioral_experiments_screen\.dart:.*\.behavioralExperimentText\('[a-zA-Z]+'
# Stable selector keys mapped to localized Exposure Reflection copy in ARB.
^lib/mobile/screens/exposure_reflection_screen\.dart:.*\.exposureReflectionText\('[a-zA-Z]+'

# Stable selector keys mapped to localized Implementation Intentions copy.
^lib/mobile/screens/implementation_intentions_screen\.dart:.*\.implementationIntentionText\('[a-zA-Z]+'

# Stable selector keys mapped to localized Uncertainty Practice and Action
# Planner copy. These are never displayed or persisted.
^lib/mobile/screens/uncertainty_training_screen\.dart:.*\.uncertaintyTrainingText\('[a-zA-Z]+'
^lib/mobile/screens/action_planner_screen\.dart:.*\.actionPlanText\('[a-zA-Z]+'

# Stable Settings UI selector keys mapped to generated localized copy.
^lib/mobile/screens/settings_screen\.dart:.*\.settingsText\('[a-zA-Z]+'
^lib/mobile/main_shell\.dart:.*\.settingsText\('[a-zA-Z]+'

# Stable mobile-shell selector keys mapped to generated localized copy. These
# choose interface messages and never appear directly or enter storage.
^lib/widgets/section_intro\.dart:.*\.shellText\('[a-zA-Z]+'
^lib/mobile/main_shell\.dart:.*\.shellText\('[a-zA-Z]+'
^lib/mobile/screens/first_run_result_screen\.dart:.*\.shellText\('[a-zA-Z]+'
^lib/mobile/widgets/spotlight_tour\.dart:.*\.shellText\('[a-zA-Z]+'
